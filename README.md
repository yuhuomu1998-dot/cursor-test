# cursor-test

This repository is used to test the Cursor + GitHub workflow.

## First Agent Test

The first Cursor project rule has been added at:

```text
.cursor/rules/first-agent.mdc
```

Expected behavior:

- Cursor can open this repository.
- The project rule is available to the agent.
- Small repository changes can be committed and pushed to GitHub.
